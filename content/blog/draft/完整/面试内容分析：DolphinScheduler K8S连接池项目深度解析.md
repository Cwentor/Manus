---
title: 面试内容分析：DolphinScheduler K8S连接池项目深度解析
summary: 面试官你好，我是广东工业大学的陈文涛。在校期间有四项项目经历：
tags:
  - 随笔
  - 完整
read_time: 11 min
slug: 面试内容分析dolphinscheduler-k8s连接池项目深度解析
category: draft
---
# 面试内容分析：DolphinScheduler K8S连接池项目深度解析

### 一、面试内容清洗（修正口误术语，梳理通顺对话）

#### 自我介绍

面试官你好，我是广东工业大学的陈文涛。在校期间有四项项目经历：

1. **Coding Agent 设计与开发**：实现了 ReAct 循环流程、MCP 工具调用、对话上下文压缩等核心能力。

2. **DolphinScheduler K8S 连接池优化**：解决 Agent 创建 K8S 连接实例冗余的问题，实现连接复用。

3. **Bigtop Manager 开源实习**：完成主机指标采集能力开发，提供 API 给前端做可视化展示。

4. **电商实时离线一体化数仓**：完整搭建数仓架构，覆盖数据采集、离线 / 实时数据处理、数仓存储与 BI 可视化全流程。

---

#### 核心面试问答

**问：详细说一下 DolphinScheduler 与 K8S 连接池项目，做了什么、解决了什么问题，遇到过哪些困难？**
答：这是一个开源项目，我和导师对接完成。
原有问题是：DolphinScheduler 每执行一个 K8S 任务就创建一个连接实例，任务结束后直接销毁，造成资源浪费。负责人希望任务执行完后，K8S 连接可以保持活跃、被复用，因此提出了构建连接池的设计思路。
我的实现是：通过 PoolConfig 标记每个连接池的唯一性，基于 K8sUtils 连接工厂创建对应 K8S 集群的连接；任务调度时从工厂获取对应集群的连接实例，使用完之后释放资源、放回连接池复用。
遇到的主要问题是沟通偏差：一开始我以为要在连接池里创建多个独立连接实例，后来负责人才说明，是要基于 DolphinScheduler 底层原有的连接创建逻辑做复用，而非创建多个孤立连接。

**问：连接是如何实现复用的？请求过来怎么从连接池取连接？通过什么机制返回连接给调用者？**
答：具体实现细节我不太记得了，我负责开发 K8S 集群的 Client Pool，通过 K8sUtils 调用工厂创建连接；任务执行完成断开连接时，应该是通过 try\-catch 把连接放回连接池。

**问：多个线程同时取连接、同时回收，怎么保证连接正确分配？会不会有线程冲突、线程安全问题？**
答：线程安全方面，我记得是用了异步线程的 key 队列做连接池管理，基于集群配置文件生成 hash code 做集群识别，多并发的情况我不太清楚。

**问：连接池的本质是什么？和每次新建连接有什么区别？为什么要用连接池？**
答：好处是省去多次创建连接实例的资源浪费，连接会一直保存在池里保持活跃，只有 K8S 的认证配置文件过期时，才会尝试重连。如果连接失败，就更新配置，废弃旧连接、用新配置创建连接。
如果是非证书原因导致的连接失效，也会废弃连接，后续有新请求时再重新构建。
连接池的本质就是一个容器，存放常用连接，用的时候取、不用的时候放回去，省去每次任务都创建 \- 销毁连接的开销。

**问：你说的过期是什么意思？**
答：一开始我没考虑过期问题，是导师提醒 K8S 的 kubeconfig 配置有一年的认证有效期，要解决认证过期后旧配置无法连接集群的问题。
我的设计思路是：如果配置文件发生变更，先保留旧连接一段时间；如果连接多次失败，就废弃旧连接。

**问：如果取到的连接已经挂掉了，后续怎么维护？比如拿到连接出现 broken pipe 异常怎么办？K8S 集群重启后连接失效，除了重启程序还有什么办法？**
答：集群重启的情况我好像确实没考虑到。
我现在的思路是，增加一个旁路线程对连接池里的连接做心跳检测，判断 K8S 集群是否活跃，同时给管理员发告警，让工作人员去后台检查集群状态、做重启处理。

**问：我的意思是，K8S 只是重启一次，连接失效了，系统怎么自愈？连接是长连接，集群重启后肯定断了，怎么自动恢复？**
答：我能想到的是，如果连接失效导致任务执行失败，就让 DolphinScheduler 重新跑一次任务；重跑的时候就会重新建立连接，再把新连接放入连接池，相当于让连接恢复活跃。

**问：怎么重新建立连接？用什么机制感知失效？客户端拿到连接只会报错，不会自动重连，你怎么处理？**
答：自动处理我想不出更好的方法，我更倾向于人工重跑任务，重跑就会重新走创建连接、放入连接池、执行任务的流程。

**问：连接池里已经有失效的连接了，不是空的，不会触发新建连接啊。**
答：确实没什么好的想法。

（面试官提示：常规方案是两种，一是旁路线程巡检销毁重建，二是取连接时先做可用性检测，失效就销毁重建，数据库连接池都是这个逻辑。候选人表示之前没考虑过、了解较浅。）

**问：你这个改造按这个思路，开源社区肯定不会接受合并不进去对吧？你们导师没有审核这个问题吗？**
答：最后确实没合并进去，当时时间紧，负责人一直没时间看 PR，提的建议也都是细枝末节的修改，没涉及到这方面。活动结束之后 PR 就关闭了。

**问：再说说电商数仓项目，你做了什么？**
答：这是我自己的学习项目，主要是熟悉数仓全流程构建，包括分层建模、数据流转：从数据采集、清洗处理构建 DWD 层，再做维度建模构建 DIM 层，按业务主题做 DWS 层，最后到 ADS 层暴露指标，支撑实时和离线的 BI 调度。

**问：Bigtop Manager 项目也是做类似的事吗？**
答：不一样，这个项目比较新，是导师做的原型，我和前端同学一起开发主机资源监控功能。基于 Prometheus 做主机资源采集，包括 CPU、内存、磁盘 IO 这些，把数据暴露给前端做可视化开发。我主要负责采集频率设置、PromQL 查询相关的开发。

**问：采集的数据怎么存到 Prometheus 里，最后怎么展现？整体逻辑是什么样的？**
答：项目要求实时性，我按配置的频率（1 秒、3 秒、5 秒）采集一次，攒一批数据提交到 Prometheus；再通过 Prometheus 的查询接口把数据暴露给前端，前端用 JavaScript 做开发。

**问：具体是怎么存储的？能存多少数据？时序数据怎么存取、怎么检索？**
答：时序数据主要用 Prometheus 自带的查询方式，数据也存在 Prometheus 里，是通过 API 开发来实现的。

**问：这个系统能解决什么问题？**
答：整个项目主要是辅助大数据组件的集群部署，以及对各集群主机的资源监控，类似 Ambari 的定位。组件安装的部分我没负责，也不太了解，我只做了主机监控。

**问：你怎么把这套系统部署起来的？**
答：Bigtop Manager 涉及组件很多，有配套的讲解视频和脚本，根据脚本做对应版本的启动，在集群上部署。我在自己主机上写 PromQL 做查询，我只负责资源采集部分，体量不大，没涉及组件安装。

**问：你有什么要向我了解的？**
答：想问一下公司现在这个岗位，在 Agent 或者 AI 应用方面的发展前景怎么样？

**问：AI 现在是所有系统的标配，是基础技能。你们有用 Agent 做开发吗？**
答：做过一个小型的 Coding Agent 项目，最小化模拟了 Claude Code 的能力，包括完整的 ReAct 循环、工具链调用、危险指令拦截、Todo List 实现、高危命令的用户确认、长对话上下文压缩、MCP 工具调用这些。

**问：怎么对危险指令进行拦截？**
答：比如 rm \-rf 这种高危指令，我在代码里写了默认的 Hook 做权限检查，硬编码了几类危险指令，匹配到就直接拦截。
如果是删除文件这类高危操作，会通过另一个 Hook 做权限确认，让用户确认是否执行，类似 Trae CN 里的用户确认环节。

**问：假如 Agent 不是用来写代码，而是用来部署 Bigtop Manager 集群，你觉得能让它随意执行指令吗？还是只能执行规定的指令？**
答：如果要做部署集群的 Agent，现在这个版本肯定支撑不了，需要补充工具链、MCP 工具和命令集。如果要做协助部署的 Agent，我会在工具集里提前加入必要的指令，LLM 调用的时候就能看到这些可用工具，知道哪些可以调用。

**问：那你觉得应该限定它能执行的命令，还是让它自己决定？生产系统里能允许 Agent 随意执行命令吗？**
答：肯定不能随意执行。
（面试官补充：生产环境运维人员都有严格权限限制，更不用说 Agent。候选人未继续深入回答。）

**问：还有其他问题吗？**
答：如果这次通过的话，后面还有面试吗？
**面试官：通过的话肯定还有。**
答：没什么问题了，谢谢。

---

### 二、面试未通过的核心原因分析

#### 1\. 核心项目深度严重不足，技术原理浮于表面（最主要原因）

你简历上最有竞争力的是 DolphinScheduler 连接池优化项目，但面试中暴露了**只做了表层功能，完全没吃透连接池的核心设计**：

- 连接池最基础的「连接分配回收机制、线程安全保障、失效检测、故障自愈」四大核心问题，你全部答不清楚，甚至实现细节都表示 “记不清了”，直接证明你对项目的参与深度有限，没有理解设计本质。

- 面对 “连接失效如何自愈” 这个工程领域的常识问题，你第一反应是 “人工重跑任务”，完全没有自动化工程思维；直到面试官提示，才知道有巡检检测、取用前校验的常规方案，暴露出你既没有主动思考过通用工程问题，也没有参考过成熟方案（如数据库连接池）的设计思路。

- 项目最终未合并入开源社区，且负责人未对核心设计做审核，进一步降低了项目的说服力 —— 面试官会认为你只是完成了一个 “能用但不合格” 的原型，不具备生产级开发能力。

#### 2\. 技术知识体系有明显漏洞，基础掌握不扎实

多个方向的问题都暴露出 “会用但不懂原理” 的问题：

- **大数据方向**：Prometheus 作为你项目里的核心组件，你说不清楚它的时序存储原理、数据检索机制、存储策略，只停留在 “写 PromQL、调 API” 的表层；电商数仓项目的回答也只是复述了 “分层建模” 的概念，没有任何具体的设计难点、性能优化、数据质量治理的细节，看不出实际动手深度。

- **Java 工程方向**：连接池是 Java 后端的经典组件，你既不了解通用实现逻辑，也没考虑线程安全、资源隔离、失效兜底这些工程必备点，和你简历上 “精通 Java、具备微服务架构能力” 的描述有明显落差。

#### 3\. 技术视野与问题解决能力偏弱，缺乏生产环境思维

- 面对 Agent 权限管控的问题，你只能想到 “硬编码黑名单” 的初级方案，没有白名单机制、权限分级、沙箱隔离、操作审计这类更体系化的生产级安全思路，对生产环境的风险认知不足。

- 遇到超出自己原有设计的问题时，临场推导能力弱，多次出现 “想不出更好的方法”“确实没什么想法” 的表述，给面试官留下 “只会做需求，不会解决问题” 的印象。

#### 4\. 表达与面试策略存在明显短板

- **回答信息密度低**：自我介绍和项目介绍卡顿、重复多，没有突出个人贡献和量化成果，大量 “记不清了”“不太清楚” 的表述，严重削弱可信度。

- **反向提问质量低**：反问环节只问了空泛的 “发展前景”，没有针对团队业务、技术栈、岗位核心挑战做深入提问；最后询问 “还有没有下一轮面试”，显得对求职流程准备不足，也没有体现出对岗位的强烈诉求。

---

### 三、核心改进方向（针对这类技术面）

1. **项目深挖**：把每个项目的「核心问题、设计方案、技术难点、踩过的坑、优化思路、最终收益」全部梳理清楚，尤其是连接池这类经典组件，必须对齐行业通用实现原理，不能只停留在 “我写了代码” 的层面。

2. **补全原理**：对项目中用到的所有核心组件（Flink、Prometheus、K8S Client 等），至少掌握核心架构、存储 / 运行原理、常见优化方案，避免 “只会用不懂原理”。

3. **强化工程思维**：所有功能设计都要配套考虑「异常怎么处理、故障怎么自愈、安全怎么保障、性能怎么优化」，这是区分 “学生项目” 和 “生产级开发” 的关键。

需要我帮你把 DolphinScheduler 连接池项目的面试回答重新整理成完整、有深度的标准答案吗？

> （注：部分内容可能由 AI 生成）
